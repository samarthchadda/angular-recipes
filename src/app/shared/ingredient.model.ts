export class Ingredient
{
    // public name:string;
    // public amount:number;

    constructor(public name:string, public amount:number)   //this syntax will automatically create properties(with name of specified arguments)
    {
        // this.name = nm;
        // this.amount = amt;
    }
}